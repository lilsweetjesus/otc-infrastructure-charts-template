apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: {{.Release.Name}}
spec:
  entryPoints:
    - {{.Values.services.kasm.ingressRoute.entryPointName}}
  routes:
    - kind: Rule
      match: Host(`{{.Values.services.kasm.ingressRoute.adminDomain}}`) && PathPrefix(`{{.Values.services.kasm.ingressRoute.pathPrefix}}`)
      services:
        - name: {{ .Release.Name }}
          passHostHeader: true
          port: {{.Values.services.kasm.service.port}}